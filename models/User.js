// Modèle utilisateur minimal
// TODO: Remplacer par la logique réelle de votre base de données
const User = {
  // Ajoutez ici les méthodes ou propriétés que vos routes auth.js utilisent
  // Exemple:
  // findByEmail: async (email) => { ... }
};

module.exports = User;
